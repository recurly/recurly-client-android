language: android
dist: trusty
script: cd recurly-android-sdk tmp && gradle build connectedCheck
android:
  components:
    - build-tools-21.1.2
    - android-21
  licenses:
    - 'android-sdk-license-.+'
    - 'google-gdk-license-.+'
